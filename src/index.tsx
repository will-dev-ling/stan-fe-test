import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./App.scss";

const container = document.getElementById("app");

// Use the old root API
ReactDOM.render(<App />, container);
